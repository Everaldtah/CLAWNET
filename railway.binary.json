{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.binary"
  },
  "deploy": {
    "startCommand": "/app/clawnet",
    "healthcheckPath": "/"
  }
}
